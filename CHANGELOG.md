# Changelog

## Unreleased (2025-12-25)
- Modernize to use `aws_ec2_host` instead of deprecated `aws_dedicated_host`.
- Add Terraform and provider constraints (Terraform >= 1.5, aws >= 5).
- Add GitHub Actions CI (fmt/validate/tflint/tfsec).
- Update outputs to modern indexing syntax.
